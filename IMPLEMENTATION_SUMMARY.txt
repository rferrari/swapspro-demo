SwapsPro Widget Implementation Complete

## Summary
Implemented all widget options from SwapsPro documentation:
- Simple configuration (query string parameters)
- Advanced configuration (postMessage theme object)
- Height update handling
- All documented props: appearance, accent, radius, background, width, font, chrome, sell, buy, amount, proAddress, theme

## Files Created/Modified
1. `src/components/SwapProWidget.tsx` - Enhanced widget component
2. `app/widget/simple/page.tsx` - Simple query-string configuration demo
3. `app/widget/advanced/page.tsx` - Advanced postMessage theme configuration demo
4. `app/widget/page.tsx` - Updated original interactive demo

## Verification
- ✅ `npm run build` succeeds (exit code 0, generates 9 static pages)
- ✅ TypeScript checking passes during build
- ✅ All widget documentation options functional
- ⚠️ `next lint` shows configuration warnings due to deprecated Next.js 15+ linter (tooling issue, not code problem)

## Implementation Details
- Simple theme: Set via `appearance` prop (query string)
- Advanced theme: Set via `theme` prop (postMessage object)
- Height updates: Handled via `swapspro:height` message listener
- Theme updates: Sent via `swapspro:style` postMessage when theme prop changes
- All other props passed as query parameters to iframe src

The widget now supports both configuration methods documented in the SwapsPro widget builder.